package templates

import "fmt"

templ ErrorPage(code int, message string) {
    @Layout(fmt.Sprintf("%d", code)){
        <hgroup class="error">
            <h1 class="pico-color-red-550">{ fmt.Sprintf("error %d", code) }</h1>
            <h3>{ message }</h3>
        </hgroup>
    }
}